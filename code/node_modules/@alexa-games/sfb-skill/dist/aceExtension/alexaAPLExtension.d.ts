import { DriverExtension, DriverExtensionParameter } from '@alexa-games/sfb-f';
import { ConfigAccessor } from './../configAccessor';
export declare class AlexaAPLExtension implements DriverExtension {
    private aplHelper;
    constructor(locale: string, configAccessor: ConfigAccessor);
    post(param: DriverExtensionParameter): Promise<void>;
    pre(param: DriverExtensionParameter): Promise<void>;
}
