import { HandlerInput } from 'ask-sdk-core';
import { VisualOptions } from '@alexa-games/sfb-f';
import { ConfigAccessor } from '../configAccessor';
export declare class APLHelper {
    private locale;
    private configAccessor;
    private templates;
    constructor(locale: string, configAccessor: ConfigAccessor);
    supportsDisplay(handlerInput: HandlerInput): import("ask-sdk-model").interfaces.viewport.ViewportState | undefined;
    generateAPLDirectiveWithVisualOptions(visualProperties: VisualOptions[]): any[];
}
