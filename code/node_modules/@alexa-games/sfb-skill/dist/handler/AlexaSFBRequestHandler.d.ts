import { HandlerInput, RequestHandler } from 'ask-sdk-core';
import { Response } from 'ask-sdk-model';
import { StoryMetadata, DriverExtension, ImporterExtension, InstructionExtension } from '@alexa-games/sfb-f';
import { ConfigAccessor } from '../configAccessor';
declare type ExtensionType = DriverExtension | ImporterExtension | InstructionExtension;
export interface SFBHandlerConfig {
    locale: string;
    story: StoryMetadata;
    pollyVoiceConfig: any;
    defaultVoiceConfig: any;
    attributeTableName: any;
}
export declare class SFBRequestHandler implements RequestHandler {
    protected config: SFBHandlerConfig;
    protected customExtensions: ExtensionType[];
    protected configAccessor: ConfigAccessor;
    protected projectDir: string;
    constructor(config: SFBHandlerConfig, customExtensions: ExtensionType[], configAccessor: ConfigAccessor, projectDir: string);
    canHandle(handlerInput: HandlerInput): boolean;
    getTableName(): string;
    handle(handlerInput: HandlerInput): Promise<Response>;
    protected isPausingRequest(handlerInput: HandlerInput): boolean;
    private buildPersistentAttributeErrorResponse;
}
export {};
