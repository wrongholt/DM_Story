"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const interfaces_1 = require("./interfaces");
class SceneDirectionBuilder {
    constructor() {
        this.sceneDirections = [];
        this.nestingStack = [];
    }
    registerChoiceDirections(utterances, directions, narration, saveToHistory = true) {
        let parameters = {
            utterances: utterances,
            saveToHistory: String(saveToHistory),
            directions: directions
        };
        if (narration) {
            parameters.narration = narration;
        }
        this.sceneDirections.push({
            directionType: interfaces_1.SceneDirectionType.CHOICE,
            parameters: parameters
        });
        return this;
    }
    startChoice(utterances, narration, saveToHistory = true) {
        let parameters = {
            utterances: utterances,
            saveToHistory: String(saveToHistory)
        };
        if (narration) {
            parameters.narration = narration;
        }
        let direction = {
            directionType: interfaces_1.SceneDirectionType.CHOICE,
            parameters: parameters
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
        this.nestingStack.push(direction);
        return this;
    }
    registerCondition(conditionString, directions) {
        let parameters = {
            condition: conditionString,
            directions: directions
        };
        this.sceneDirections.push({
            directionType: interfaces_1.SceneDirectionType.CONDITION,
            parameters: parameters
        });
        return this;
    }
    startCondition(conditionString) {
        let parameters = {
            condition: conditionString,
        };
        let direction = {
            directionType: interfaces_1.SceneDirectionType.CONDITION,
            parameters: parameters
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
        this.nestingStack.push(direction);
        return this;
    }
    goTo(targetSceneID, targetSceneProperty) {
        let parameters = {
            target: targetSceneID,
        };
        if (targetSceneProperty) {
            parameters.targetSceneProperty = targetSceneProperty;
        }
        let direction = {
            directionType: interfaces_1.SceneDirectionType.GO_TO,
            parameters: parameters
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
        return this;
    }
    saveAndGoTo(targetSceneID, targetSceneProperty) {
        let parameters = {
            target: targetSceneID,
        };
        if (targetSceneProperty) {
            parameters.targetSceneProperty = targetSceneProperty;
        }
        let direction = {
            directionType: interfaces_1.SceneDirectionType.SAVE_AND_GO,
            parameters: parameters
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
        return this;
    }
    return() {
        let direction = {
            directionType: interfaces_1.SceneDirectionType.RETURN,
            parameters: {}
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
        return this;
    }
    pause() {
        let direction = {
            directionType: interfaces_1.SceneDirectionType.PAUSE,
            parameters: {}
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
        return this;
    }
    restart() {
        let direction = {
            directionType: interfaces_1.SceneDirectionType.RESTART,
            parameters: {}
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
        return this;
    }
    repeat() {
        let direction = {
            directionType: interfaces_1.SceneDirectionType.REPEAT,
            parameters: {}
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
        return this;
    }
    repeatReprompt() {
        let direction = {
            directionType: interfaces_1.SceneDirectionType.REPEAT_REPROMPT,
            parameters: {}
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
        return this;
    }
    goBack(backNumber) {
        let direction = {
            directionType: interfaces_1.SceneDirectionType.BACK,
            parameters: {
                count: backNumber
            }
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
        return this;
    }
    setReprompt(repromptSSML) {
        let direction = {
            directionType: interfaces_1.SceneDirectionType.REPROMPT,
            parameters: {
                message: repromptSSML,
            }
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
        return this;
    }
    setBackgroundMusic(audioURL, delayMs, volume, blendOption) {
        let param = {
            audioURL: audioURL
        };
        if (delayMs)
            param.delayMs = delayMs;
        if (volume)
            param.volume = volume;
        if (blendOption)
            param.blend = blendOption;
        let direction = {
            directionType: interfaces_1.SceneDirectionType.BGM,
            parameters: param
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
    }
    setVisuals(visualOptions) {
        let params = {};
        Object.keys(visualOptions).forEach(function (key) {
            params[key] = visualOptions[key];
        });
        this.sceneDirections.push({
            directionType: interfaces_1.SceneDirectionType.VISUALS,
            parameters: params
        });
        return this;
    }
    increaseVariable(variableName, increaseBy) {
        let direction = {
            directionType: interfaces_1.SceneDirectionType.INCREASE,
            parameters: {
                variableName: variableName,
                variableValue: increaseBy
            }
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
        return this;
    }
    multiplyVariable(variableName, multiplyBy) {
        let direction = {
            directionType: interfaces_1.SceneDirectionType.MULTIPLY,
            parameters: {
                variableName: variableName,
                variableValue: multiplyBy
            }
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
        return this;
    }
    divideVariable(variableName, divideBy) {
        let direction = {
            directionType: interfaces_1.SceneDirectionType.DIVIDE,
            parameters: {
                variableName: variableName,
                variableValue: divideBy
            }
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
        return this;
    }
    reduceVariable(variableName, reduceBy) {
        let direction = {
            directionType: interfaces_1.SceneDirectionType.REDUCE,
            parameters: {
                variableName: variableName,
                variableValue: reduceBy
            }
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
        return this;
    }
    setVariable(variableName, value) {
        let direction = {
            directionType: interfaces_1.SceneDirectionType.SET,
            parameters: {
                variableName: variableName,
                variableValue: value
            }
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
        return this;
    }
    setSlot(variableName, slotType) {
        let direction = {
            directionType: interfaces_1.SceneDirectionType.SLOT,
            parameters: {
                variableName: variableName,
                variableType: slotType
            }
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
        return this;
    }
    flag(variableName) {
        let direction = {
            directionType: interfaces_1.SceneDirectionType.FLAG,
            parameters: {
                variableName: variableName
            }
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
        return this;
    }
    unflag(variableName) {
        let direction = {
            directionType: interfaces_1.SceneDirectionType.UNFLAG,
            parameters: {
                variableName: variableName
            }
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
        return this;
    }
    clearVariable(variableName) {
        let direction = {
            directionType: interfaces_1.SceneDirectionType.CLEAR,
            parameters: {
                variableName: variableName
            }
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
        return this;
    }
    setRecap(message) {
        let direction = {
            directionType: interfaces_1.SceneDirectionType.RECAP,
            parameters: {
                message: message,
            }
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
        return this;
    }
    addToInventory(variableName, item) {
        let param = {
            variableName: variableName,
            itemName: item,
        };
        let direction = {
            directionType: interfaces_1.SceneDirectionType.ADD_TO_INVENTORY,
            parameters: param
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
        return this;
    }
    addItem(variableName, item, listSizeLimit) {
        let param = {
            variableName: variableName,
            itemName: item,
        };
        if (listSizeLimit) {
            param.size = String(listSizeLimit);
        }
        let direction = {
            directionType: interfaces_1.SceneDirectionType.ADD_ITEM,
            parameters: param
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
        return this;
    }
    removeItem(variableName, item) {
        let direction = {
            directionType: interfaces_1.SceneDirectionType.REMOVE_ITEM,
            parameters: {
                variableName: variableName,
                itemName: item
            }
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
        return this;
    }
    removeFirstItem(listName) {
        let direction = {
            directionType: interfaces_1.SceneDirectionType.REMOVE_FIRST,
            parameters: {
                variableName: listName
            }
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
        return this;
    }
    removeLastItem(listName) {
        let direction = {
            directionType: interfaces_1.SceneDirectionType.REMOVE_LAST,
            parameters: {
                variableName: listName
            }
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
        return this;
    }
    rollDice(diceString) {
        let direction = {
            directionType: interfaces_1.SceneDirectionType.ROLL,
            parameters: {
                diceString: diceString
            }
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
        return this;
    }
    markEnding() {
        let direction = {
            directionType: interfaces_1.SceneDirectionType.END,
            parameters: {}
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
        return this;
    }
    closeChoice() {
        if (this.nestingStack.length > 0) {
            this.nestingStack.splice(this.nestingStack.length - 1, 1);
        }
        return this;
    }
    closeCondition() {
        if (this.nestingStack.length > 0) {
            this.nestingStack.splice(this.nestingStack.length - 1, 1);
        }
        return this;
    }
    addSceneDirection(direction) {
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(JSON.parse(JSON.stringify(direction)));
        }
        return this;
    }
    customDirection(customDirectionName, customParameters = {}) {
        let direction = {
            directionType: interfaces_1.SceneDirectionType.CUSTOM,
            parameters: Object.assign({
                customName: customDirectionName
            }, customParameters)
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
        return this;
    }
    getCurrentEpochTime() {
        let direction = {
            directionType: interfaces_1.SceneDirectionType.GET_TIME,
            parameters: {}
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
        return this;
    }
    setBookmark(bookmarkName, sceneName) {
        let direction = {
            directionType: interfaces_1.SceneDirectionType.BOOKMARK,
            parameters: {
                variableName: bookmarkName,
                variableValue: sceneName
            }
        };
        if (this.nestingStack.length > 0) {
            if (!this.nestingStack[this.nestingStack.length - 1].parameters.directions) {
                this.nestingStack[this.nestingStack.length - 1].parameters.directions = [];
            }
            this.nestingStack[this.nestingStack.length - 1].parameters.directions.push(direction);
        }
        else {
            this.sceneDirections.push(direction);
        }
        return this;
    }
    build() {
        return this.sceneDirections;
    }
}
exports.SceneDirectionBuilder = SceneDirectionBuilder;
//# sourceMappingURL=SceneDirectionBuilder.js.map