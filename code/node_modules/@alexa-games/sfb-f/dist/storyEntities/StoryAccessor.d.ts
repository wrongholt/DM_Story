import { StoryMetadata, Scene, SceneDirection } from "../aceEntities/interfaces";
/**
 * Accessor / Helper for StoryMetadata object.
 */
export declare class StoryAccessor {
    protected story: {
        [key: string]: Scene;
    };
    protected sourceData: StoryMetadata;
    constructor(story: StoryMetadata);
    /**
     * Get the scene object of the currently driving story givene the scene ID.
     * @param sceneID scene ID
     */
    getSceneByID(sceneID: string): Scene;
    getSceneNarration(sceneID: string): string;
    getSceneInstructions(sceneID: string): SceneDirection[];
    /**
     * Gets a list of Scenes within the current story.
     */
    getAllScenes(): Scene[];
    getStoryID(): Promise<string>;
    getStoryMetadata(): Promise<StoryMetadata>;
}
