"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const interfaces_1 = require("../aceEntities/interfaces");
class AlexaABCVerifier {
    async verify(story) {
        return new Promise((resolve, reject) => {
            let foundErrors = [];
            let hasStart = false;
            for (let scene of story.scenes) {
                if (scene.id.trim().toLowerCase() == interfaces_1.BuiltInScenes.StartScene.trim().toLowerCase()) {
                    hasStart = true;
                }
            }
            if (!hasStart) {
                foundErrors.push({
                    lineNumber: 0,
                    errorName: "MissingScene",
                    errorMessage: "Cannot find the required scene 'start'."
                });
            }
            resolve(foundErrors);
        });
    }
}
exports.AlexaABCVerifier = AlexaABCVerifier;
//# sourceMappingURL=alexa-abc-verifier.js.map