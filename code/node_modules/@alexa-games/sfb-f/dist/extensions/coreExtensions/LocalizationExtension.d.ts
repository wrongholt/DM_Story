import { ImporterExtension } from '../ACEExtension';
import { SourceContentHelper } from '../../importPlugins/sourceContentHelper';
import { StoryMetadataHelper } from '../../importPlugins/storyMetadataHelper';
/**
 * Creates a string ID for each chunk of localizable strings, and replaces them.
 */
export declare class LocalizationExtension implements ImporterExtension {
    private locale;
    private languageString;
    private overwrite;
    private srcLocale;
    constructor(locale: string, languageString: any, overwrite?: boolean, srcLocale?: string);
    extendSourceContent(sourceHelper: SourceContentHelper): Promise<void>;
    extendImportedContent(metadataHelper: StoryMetadataHelper): Promise<void>;
    getLocalizedStringsObject(): any;
    private updateLanguageString;
}
