import { StoryAccessor } from "../storyEntities/StoryAccessor";
import { SceneDirection, Scene, StoryMetadata } from '../aceEntities/interfaces';
/**
 * Help access and navigate the StoryMetadata used by the Driver
 */
export declare class StoryMetadataHelper extends StoryAccessor {
    constructor(story: StoryMetadata);
    /**
     * Set and replace all Scenes for the current import result.
     */
    setAllScenes(scenes: Scene[]): void;
    /**
     * Set and replace a scene for the current import result.
     */
    setScene(sceneID: string, narration?: string, instructions?: SceneDirection[], customSceneProperty?: any): void;
    /**
     * Set and replace the narration property of the given scene.
     */
    setSceneNarration(sceneID: string, narration: string): void;
    /**
     * Set and replace instructions for the given scene.
     */
    setSeceneInstructions(sceneID: string, instructions: SceneDirection[]): void;
}
